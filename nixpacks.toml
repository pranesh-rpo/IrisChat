[phases.setup]
nixPkgs = ["python311", "ffmpeg", "gcc", "cmake", "git", "gnumake"]

[phases.install]
cmds = [
    "python -m venv .venv",
    ". .venv/bin/activate && pip install -r requirements.txt"
]

[start]
cmd = ". .venv/bin/activate && python main.py"
